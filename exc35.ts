const animal:string[]=["Lion","Tiger ","Leopard"];
for(let i:number=0;i<animal.length;i++)
{
    console.log(`${animal[i]} is known for its strength`);
}
console.log("All of them have great strength,sharp claws and they all are fast runners");